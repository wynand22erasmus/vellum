# Function: cn()

> **cn**(...`inputs`): `string`

Defined in: [src/lib/utils.ts:16](https://github.com/wynand22erasmus/vellum/blob/df8dd981b8ffb4c4991a6666cd26c449bfb192b3/src/lib/utils.ts#L16)

Merges Tailwind class names with `clsx` and resolves conflicts via `tailwind-merge`.

## Parameters

### inputs

...`ClassValue`[]

Class name fragments or conditional maps

## Returns

`string`

Combined class string
