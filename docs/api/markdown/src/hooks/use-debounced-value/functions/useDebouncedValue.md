# Function: useDebouncedValue()

> **useDebouncedValue**\<`T`\>(`value`, `delayMs`): `T`

Defined in: [src/hooks/use-debounced-value.ts:12](https://github.com/wynand22erasmus/vellum/blob/df8dd981b8ffb4c4991a6666cd26c449bfb192b3/src/hooks/use-debounced-value.ts#L12)

Returns a copy of `value` that updates only after `delayMs` without changes.

## Type Parameters

### T

`T`

## Parameters

### value

`T`

### delayMs

`number`

## Returns

`T`
