/**
 * Public auth API surface (session user type and safe redirect helper).
 *
 * @packageDocumentation
 */

export { safeReturnTo } from '@/lib/auth/returnTo.ts';
export type { AuthUser } from '@/lib/auth/types.ts';
