meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/auth/logout
  body: none
  auth: none
}

tests {
  test("logout succeeds", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().message).to.contain("Logged out");
  });
}
