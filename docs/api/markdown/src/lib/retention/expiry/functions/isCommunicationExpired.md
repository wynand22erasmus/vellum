# Function: isCommunicationExpired()

> **isCommunicationExpired**(`link`, `now`): `boolean`

Defined in: src/lib/retention/expiry.ts:12

## Parameters

### link

`Pick`\<`Communication`, `"linkExpiresAt"` \| `"revokedAt"`\>

### now

`Date`

## Returns

`boolean`
