meta {
  name: Verify Missing Fields
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/verify
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {}
}

tests {
  test("requires token and password", function () {
    expect(res.getStatus()).to.equal(400);
    expect(res.getBody().error).to.contain("required");
  });
}
