# Function: ensureSftpDirectories()

> **ensureSftpDirectories**(): `Promise`\<`void`\>

Defined in: src/server/sftp/inboxWatcher.ts:27

Ensures inbox, archive, and failed directories exist.

## Returns

`Promise`\<`void`\>
