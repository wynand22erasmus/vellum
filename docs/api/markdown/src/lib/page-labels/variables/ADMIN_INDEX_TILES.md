# Variable: ADMIN\_INDEX\_TILES

> `const` **ADMIN\_INDEX\_TILES**: readonly \[\{ `description`: `"Shared file assets in object storage (deduplicated by SHA-256)."`; `href`: `"/admin/document-files"`; `id`: `"admin-document-files"`; `label`: `"Document files"`; \}, \{ `description`: `"Per-recipient download links, passwords, and lifecycle state."`; `href`: `"/admin/documents"`; `id`: `"admin-documents"`; `label`: `"Document links"`; \}, \{ `description`: `"Dashboard accounts and roles."`; `href`: `"/admin/users"`; `id`: `"admin-users"`; `label`: `"Users"`; \}, \{ `description`: `"Compliance events with optional filters."`; `href`: `"/admin/audit-logs"`; `id`: `"admin-audit"`; `label`: `"Audit logs"`; \}, \{ `description`: `"Dead-letter payloads when audit enqueue fails."`; `href`: `"/admin/failed-audit-logs"`; `id`: `"admin-failed-audit"`; `label`: `"Failed audit logs"`; \}, \{ `description`: `"RFC 9457 operational failures from HTTP, workers, and queues."`; `href`: `"/admin/process-errors"`; `id`: `"admin-process-errors"`; `label`: `"Process errors"`; \}, \{ `description`: `"Dead-letter payloads when process-error enqueue fails."`; `href`: `"/admin/failed-process-errors"`; `id`: `"admin-failed-process-errors"`; `label`: `"Failed process errors"`; \}, \{ `description`: `"Outbound audit webhook HTTP attempts."`; `href`: `"/admin/webhook-deliveries"`; `id`: `"admin-webhook-deliveries"`; `label`: `"Webhook deliveries"`; \}, \{ `description`: `"Dead-letter payloads when webhook delivery exhausts retries."`; `href`: `"/admin/failed-webhook-deliveries"`; `id`: `"admin-failed-webhook-deliveries"`; `label`: `"Failed webhook deliveries"`; \}\]

Defined in: [src/lib/page-labels.ts:135](https://github.com/wynand22erasmus/vellum/blob/02442fedaf00245060c21695c36ee0c2b9e841c3/src/lib/page-labels.ts#L135)

Admin overview tiles linking to each admin sub-section.
