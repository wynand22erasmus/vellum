# Function: purgeExpiredRecords()

> **purgeExpiredRecords**(`now?`): `Promise`\<`void`\>

Defined in: src/lib/retention/purge.ts:18

Purges expired audit, link, document, file, and orphan recipient rows.

## Parameters

### now?

`Date` = `...`

## Returns

`Promise`\<`void`\>
