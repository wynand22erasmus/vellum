meta {
  name: Revoke Document
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/documents/{{documentId}}/revoke
  body: none
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
