# Function: useDebouncedValue()

> **useDebouncedValue**\<`T`\>(`value`, `delayMs`): `T`

Defined in: [src/hooks/use-debounced-value.ts:12](https://github.com/wynand22erasmus/vellum/blob/02442fedaf00245060c21695c36ee0c2b9e841c3/src/hooks/use-debounced-value.ts#L12)

Returns a copy of `value` that updates only after `delayMs` without changes.

## Type Parameters

### T

`T`

## Parameters

### value

`T`

### delayMs

`number`

## Returns

`T`
