meta {
  name: Vellum API
}

auth {
  mode: none
}

vars:pre-request {
  baseUrl: http://localhost:5173
  apiKey: dev-api-key-change-in-production
}

docs {
  API tests for Vellum. Run `npm run test:e2e:seed` before verify/document tests that need a live document.
}
