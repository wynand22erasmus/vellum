# Function: createApp()

> **createApp**(): `Promise`\<`Application`\>

Defined in: [src/server/create-app.ts:40](https://github.com/wynand22erasmus/vellum/blob/02442fedaf00245060c21695c36ee0c2b9e841c3/src/server/create-app.ts#L40)

Builds the Express app with security middleware and mounted API routers.

## Returns

`Promise`\<`Application`\>

Configured Express application (SPA in production)
