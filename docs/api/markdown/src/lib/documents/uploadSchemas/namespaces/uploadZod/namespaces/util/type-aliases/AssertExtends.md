# Type Alias: AssertExtends\<T, U\>

> **AssertExtends**\<`T`, `U`\> = `T` *extends* `U` ? `T` : `never`

Defined in: node\_modules/zod/v4/core/util.d.cts:17

## Type Parameters

### T

`T`

### U

`U`
