# Variable: unicodeEmail

> `const` **unicodeEmail**: `RegExp`

Defined in: node\_modules/zod/v4/core/regexes.d.cts:32

A loose regex that allows Unicode characters, enforces length limits, and that's about it.
