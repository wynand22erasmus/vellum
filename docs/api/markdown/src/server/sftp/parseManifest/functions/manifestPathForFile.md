# Function: manifestPathForFile()

> **manifestPathForFile**(`filePath`, `manifestSuffix`): `string`

Defined in: src/server/sftp/parseManifest.ts:37

Resolves the manifest path for a data file in the SFTP inbox.

## Parameters

### filePath

`string`

### manifestSuffix

`string`

## Returns

`string`
