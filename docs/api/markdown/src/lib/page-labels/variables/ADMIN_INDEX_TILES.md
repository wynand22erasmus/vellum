# Variable: ADMIN\_INDEX\_TILES

> `const` **ADMIN\_INDEX\_TILES**: readonly \[\{ `description`: `"Shared file assets in object storage (deduplicated by SHA-256)."`; `href`: `"/admin/files"`; `id`: `"admin-files"`; `label`: `"Files"`; \}, \{ `description`: `"Delivery envelopes pairing a file with a recipient (password, download limits)."`; `href`: `"/admin/documents"`; `id`: `"admin-documents"`; `label`: `"Documents"`; \}, \{ `description`: `"Recipient identities with delivery contact and OTP channel preferences."`; `href`: `"/admin/recipients"`; `id`: `"admin-recipients"`; `label`: `"Recipients"`; \}, \{ `description`: `"Outbound verify URLs with expiry and revocation state per envelope."`; `href`: `"/admin/communications"`; `id`: `"admin-communications"`; `label`: `"Communications"`; \}, \{ `description`: `"Dashboard accounts and roles."`; `href`: `"/admin/users"`; `id`: `"admin-users"`; `label`: `"Users"`; \}, \{ `description`: `"Compliance events with optional filters."`; `href`: `"/admin/audit-logs"`; `id`: `"admin-audit"`; `label`: `"Audit logs"`; \}, \{ `description`: `"Failed queue payloads from audit, process-error, and webhook pipelines."`; `href`: `"/admin/dead-letters"`; `id`: `"admin-dead-letters"`; `label`: `"Dead letters"`; \}, \{ `description`: `"RFC 9457 operational failures from HTTP, workers, and queues."`; `href`: `"/admin/process-errors"`; `id`: `"admin-process-errors"`; `label`: `"Process errors"`; \}, \{ `description`: `"Outbound audit webhook HTTP attempts."`; `href`: `"/admin/webhook-deliveries"`; `id`: `"admin-webhook-deliveries"`; `label`: `"Webhook deliveries"`; \}\]

Defined in: [src/lib/page-labels.ts:143](https://github.com/wynand22erasmus/vellum/blob/df8dd981b8ffb4c4991a6666cd26c449bfb192b3/src/lib/page-labels.ts#L143)

Admin overview tiles linking to each admin sub-section.
