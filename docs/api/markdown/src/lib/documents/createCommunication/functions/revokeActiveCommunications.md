# Function: revokeActiveCommunications()

> **revokeActiveCommunications**(`documentId`, `now?`): `Promise`\<`string`[]\>

Defined in: src/lib/documents/createCommunication.ts:72

Revokes all active links on a document envelope.

## Parameters

### documentId

`string`

### now?

`Date` = `...`

## Returns

`Promise`\<`string`[]\>
