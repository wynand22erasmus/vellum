# Function: cn()

> **cn**(...`inputs`): `string`

Defined in: [src/lib/utils.ts:16](https://github.com/wynand22erasmus/vellum/blob/02442fedaf00245060c21695c36ee0c2b9e841c3/src/lib/utils.ts#L16)

Merges Tailwind class names with `clsx` and resolves conflicts via `tailwind-merge`.

## Parameters

### inputs

...`ClassValue`[]

Class name fragments or conditional maps

## Returns

`string`

Combined class string
