# Function: createApp()

> **createApp**(): `Promise`\<`Application`\>

Defined in: [src/server/create-app.ts:40](https://github.com/wynand22erasmus/vellum/blob/df8dd981b8ffb4c4991a6666cd26c449bfb192b3/src/server/create-app.ts#L40)

Builds the Express app with security middleware and mounted API routers.

## Returns

`Promise`\<`Application`\>

Configured Express application (SPA in production)
